<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="viewport-fit=cover, initial-scale=1, maximum-scale=1.5, minimum-scale=1, width=device-width">
    <meta http-equiv="Content-Security-Policy" content="default-src 'self' data: gap: https://ssl.gstatic.com 'unsafe-eval';script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; media-src *; connect-src *">
    <meta name="format-detection" content="telephone=no">
    <meta name="msapplication-tap-highlight" content="no">
    <title>Convention Showcase App | Budget Blinds - Signature Series</title>
    <link rel="stylesheet" href="{% if topLevelNav == false %}../{% endif %}assets/css/styles.min.css">
    {% block links %}{% endblock %}
  </head>
  <body data-page-left="{{ pageLeft }}" data-page-right="{{ pageRight }}">

    <div class="app">
      <div class="content-container transition-container">
        {% block content %}{% endblock %}
      </div>
    </div>

    <div class="popup-video-container">
      <video id="video" class="video" loop src="{% if topLevelNav == false %}../{% endif %}assets/videos/convention-video.mp4" width="1024" height:="768" type="video/mp4">
      </video>
    </div>

    <script src="{% if topLevelNav == false %}../{% endif %}cordova.js"></script>
    <script src="{% if topLevelNav == false %}../{% endif %}assets/js/app.min.js"></script>
    {% block scripts %}{% endblock %}

  </body>
</html>
